*Completing the menu options in Menu_CLI
	after these are done we can just use the same format for the gui just with buttons and event handling.

*Figuring out how to handle tax data, I might add second file tax.csv that hold tax data of all years of the same line as file.csv

*Big task is for proccesing the data for the stats needed in menu=>admin=>stats, should be held in CSV_Storing (or another class) so both gui and cli can access
